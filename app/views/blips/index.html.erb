<h1>Your Blips</h1>

<table class="table table-striped table-bordered table-condensed">
  <thead>
  <tr>
    <th colspan="1">Title</th>
    <th colspan="1">Summary</th>
    <th colspan="1">Link</th>
    <th colspan="1">Time</th>
    <th colspan="1">Address</th>
    <th colspan="1">Latitude</th>
    <th colspan="1">Longitude</th>
    <th colspan="1">Actions</th>
  </tr>
  </thead>
  <tbody>
  <% unless @blipsArray.empty? %>
      <% @blipsArray.each do |blipHash| %>
          <tr>
            <td><%= blipHash["title"]   %></td>
            <td><%= blipHash["summary"] %></td>
            <td><%= blipHash["link"]    %></td>
            <td><%= blipHash["time"]    %></td>
            <td><%= blipHash["address"] %></td>
            <td><%= blipHash["lat"]     %></td>
            <td><%= blipHash["lng"]     %></td>

            <td>
              <%= link_to "Show", {:action => "show" , :id => blipHash["id"]}, class: "btn btn-small" %>
              <%= link_to "Edit", {:action => "edit" , :id => blipHash["id"]}, class: "btn btn-small" %>
              <%# <%= link_to "Delete", blip, method: :delete, data: { confirm: "You sure?" } , class: "btn btn-small"%>
            </td>
          </tr>
      <% end %>
  <% else %>
      <tr>
        <td colspan="0">Send your first blip!!!</td>
      </tr>
  <% end %>
  </tbody>

</table>

  <div class="contextual">
  <%= link_to(new_blip_path, class: "btn btn-success btn-lg") do %>
      <i class="glyphicon glyphicon-plus"></i> New Blip
  <% end %>
</div>